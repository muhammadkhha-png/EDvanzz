using System;
using System.Collections.Generic;
using System.Text;

namespace Edvanz.Domain.Enums
{
    public enum SendTimingType { Immediate = 1, Scheduled = 2 }
    public enum TriggerScope { All = 1, Session = 2, SessionGroup = 3 }
}
